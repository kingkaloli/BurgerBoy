<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Moab Burger Boy Restaurant </h1>
    <p class="lead">Eat.Be Merry.Code.Climb.Live Your Life!</p>
  </div>
</div>
            <div class="container">
              <div class="row">
                  <div class="col-md-4">
                      </div>
                  <div class="col-md-4" class="task">
                 {{#each burger_data}}
                 {{#unless this.devoured}}
                 </div>
                 <div class="col-md-4">
                      </div>
                      </div>
                      </div>
                 <div class="col-md-12"
                    <pre>{{this.id}}. {{this.burger_name}}</pre>
                </div>
                <div class="col-xs-3 text center"></div>
                <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
                    <input type="hidden" name="burger_id" type="text" value={{this.id}}>
                    <br>
                    <button type="hidden" class="btn btn-outline-success"> Eat This Delicious Burger</button>
                    <br>
                </form>
            </div>
            {{/unless}}
            {{/each}}
        </div>
    </div>
    <br>
    <p>Out of Stock Burgers</p>
    
    <div class="col-xs-6 text center" class="task">
        {{#each burger_data}}
        {{#if this.devoured}}
        <input class="form-control" type="text" placeholder="{{this.id}}.{{this.burger_name}}" readonly>
        {{/if}}
        {{/each}}
    </div>
</div>
</div>
<br>
<div class="container">
    <div class="row">
        <div class="col-md-4">
            <p>Create Your Own Burger Below!</p>
              </div>
              <div class="col-md-4">
          </div>
          <div class="col-md-4">
              </div>
           </div>
            </div>
            <div class="container">
            <div class="row">
           <div class="col-md-4">
            <form action="/burgers/create" method="POST">
            <input type="text" name="burger_name" class="form-control"
            value="" id="enter_text">
            <br>
            <button type="submit" class="btn btn-outline-success">Add a Burg</button>
           </form>
           </div>
            <div class="col-md-4">
              </div>
               <div class="col-md-4">
              </div>
           
      
           
</div>
</div>

